<x-layouts.storefront title="Recherche | Passion Cosmetic" description="Recherchez un soin ou une catégorie." :canonical="route('storefront.search')">
    <section class="catalogue-hero search-page"><p class="eyebrow">Recherche</p><h1>Trouver un soin</h1><form action="{{ route('storefront.search') }}" role="search"><label class="sr-only" for="search-page-query">Votre recherche</label><input id="search-page-query" name="q" type="search" value="{{ $query }}" placeholder="Ex. huile, visage, rituel"><button class="button button-dark">Rechercher</button></form></section>
    @if($query !== '')<section class="catalogue-page section"><p class="result-count">Résultats pour « {{ $query }} »</p>@if($categories->isNotEmpty())<div class="search-category-results"><p class="eyebrow">Catégories</p>@foreach($categories as $category)<a href="{{ route('storefront.category', $category->slug) }}">{{ $category->name }} <span>→</span></a>@endforeach</div>@endif @if($products->isNotEmpty())<div class="product-grid">@foreach($products as $product)<x-storefront.product-card :product="$product" />@endforeach</div>{{ $products->links() }}@elseif($categories->isEmpty())<div class="catalogue-empty"><h2>Aucun résultat pour cette recherche.</h2><p>Essayez un autre mot ou parcourez la boutique.</p><a class="text-link" href="{{ route('storefront.products') }}">Voir tous les soins</a></div>@endif</section>@endif
</x-layouts.storefront>
