<x-layouts.storefront :title="($category->seo_title ?: $category->name).' | Passion Cosmetic'" :description="$category->seo_description ?: ($category->description ?: 'Découvrez la sélection '.$category->name.' de Passion Cosmetic.')">
    <section class="catalogue-hero"><nav class="breadcrumb" aria-label="Fil d’Ariane"><a href="{{ route('storefront.home') }}">Accueil</a><span>/</span><a href="{{ route('storefront.products') }}">Soins</a><span>/</span><span aria-current="page">{{ $category->name }}</span></nav><p class="eyebrow">Collection</p><h1>{{ $category->name }}</h1>@if($category->description)<p>{{ $category->description }}</p>@endif</section>
    <section class="catalogue-page section"><x-storefront.catalogue-filters :categories="$categories" :current-category="$category" /><p class="result-count">{{ $products->total() }} {{ Str::plural('produit', $products->total()) }}</p>@if($products->isNotEmpty())<div class="product-grid">@foreach($products as $product)<x-storefront.product-card :product="$product" />@endforeach</div>{{ $products->links() }}@else<div class="catalogue-empty"><h2>Cette collection se prépare encore.</h2><p>Découvrez les autres soins de la boutique.</p><a class="text-link" href="{{ route('storefront.products') }}">Voir tous les soins</a></div>@endif</section>
</x-layouts.storefront>
